default: &default
  adapter: postgresql
  encoding: UNICODE
  host: localhost
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: partner-metrics-dev

test:
  <<: *default
  database: partner-metrics-test
